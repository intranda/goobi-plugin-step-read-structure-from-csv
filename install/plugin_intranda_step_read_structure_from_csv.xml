<config_plugin>
    <!--
        order of configuration is:
          1.) project name and step name matches
          2.) step name matches and project is *
          3.) project name matches and step name is *
          4.) project name and step name are *
	-->
    
    <config>
        <!-- which projects to use for (can be more then one, otherwise use *) -->
        <project>*</project>
        <step>*</step>
        
        <!-- column where the identifier is used to notice in which row a new object starts -->
        <idColumn>object_id</idColumn>
        
        <!-- column where the pagination is included -->
        <paginationColumn>pageNo</paginationColumn>
        
        <!-- column where the file name is included -->
        <fileColumn>file_Name</fileColumn>
        
        <!-- mimetype to use -->
        <fileMimeType>image/tiff</fileMimeType>

        <!-- file extension use -->
        <fileExtension>.tif</fileExtension>
        
        <!-- structure type to create for each object -->
        <structureTypes>Letter</structureTypes>
        
        <!-- list of metadata fields -->
        <column header="object_title" ruleset="TitleDocMain" person="false"/>
        <column header="date" ruleset="DateOfOrigin" person="false"/>
        <column header="sender" ruleset="Author" person="true" separator=","/>
        <column header="receiver" ruleset="Recipient" person="true" separator=","/>
        
    </config>

</config_plugin>
